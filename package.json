{
  "name": "hausio",
  "version": "1.0.0",
  "private": true,
  "description": "Hausio London — home services site (cleaning, removals, handyman). Static site + Netlify Functions for Stripe deposits.",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "stripe": "^17.5.0"
  }
}
